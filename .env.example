# Base URL for the backend REST/GraphQL API.
# Currently unused — all data comes from src/data/mock and src/services.
# Point this at your backend once it exists and swap the mock service
# implementations for real fetch calls (see README.md "API integration").
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api
